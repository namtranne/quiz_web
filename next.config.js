// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: false,
	images: {
		domains: ['doidepfmcg.com'] // add all external domains you want
	}
}

module.exports = nextConfig
